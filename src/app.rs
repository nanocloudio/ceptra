use anyhow::Result;

/// Application orchestrator entrypoint. Load configuration, initialize
/// subsystems, and start the runtime loop.
pub fn run() -> Result<()> {
    // TODO: wire configuration loading, runtime setup, and main loop.
    Ok(())
}
